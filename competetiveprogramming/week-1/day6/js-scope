Answer: undefined
In js, a variable declaration is hoisted to the top of the scope before the acta value is assigned
so, implicitly the implementation will be
var text='outside'
function logit(){
	var text
	console.log(text)
	text='inside'
};
logit()